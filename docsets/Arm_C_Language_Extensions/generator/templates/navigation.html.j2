<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="{{ eyebrow }}: {{ title }}">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="../assets/style.css">
</head>
<body>
  <main class="page">
    <a name="{{ anchor }}" class="dashAnchor"></a>
    <header class="hero">
      <div class="eyebrow">{{ eyebrow }}</div>
      <h1>{{ title }}</h1>
      {% if parent %}<p><a href="{{ parent.href }}">← {{ parent.label }}</a></p>{% endif %}
    </header>

    {% if children %}
    <a name="//apple_ref/cpp/Guide/Subcategories" class="dashAnchor"></a>
    <section aria-labelledby="children-heading">
      <h2 id="children-heading">Subcategories</h2>
      <ul class="guide-list">{% for child in children %}<li><a href="{{ child.href }}">{{ child.label }}</a><span class="muted"> — {{ child.count }} callables in this branch</span><div class="guide-path">{{ child.path }}</div></li>{% endfor %}</ul>
    </section>
    {% endif %}

    {% if types %}
    <a name="//apple_ref/cpp/Guide/Documented%20data%20types" class="dashAnchor"></a>
    <section aria-labelledby="types-heading">
      <h2 id="types-heading">Documented data types</h2>
      <div class="table-scroll">
        <table>
          <thead><tr><th>Type</th><th>Derived width</th></tr></thead>
          <tbody>{% for item in types %}<tr><td data-label="Type"><a href="{{ item.href }}"><code>{{ item.name }}</code></a></td><td data-label="Derived width">{{ item.summary }}</td></tr>{% endfor %}</tbody>
        </table>
      </div>
    </section>
    {% endif %}

    <a name="//apple_ref/cpp/Guide/Direct%20callables" class="dashAnchor"></a>
    <section aria-labelledby="callables-heading">
      <h2 id="callables-heading">Direct callables</h2>
      {% if items %}
      <div class="table-scroll">
        <table>
          <thead><tr><th>Kind</th><th>Callable</th><th>Exact signature</th></tr></thead>
          <tbody>{% for item in items %}<tr><td data-label="Kind">{{ item.kind }}</td><td data-label="Callable"><a href="{{ item.href }}"><code>{{ item.name }}</code></a></td><td data-label="Exact signature"><code>{{ item.signature }}</code></td></tr>{% endfor %}</tbody>
        </table>
      </div>
      {% else %}
      <p class="missing-value">{{ empty_message }}</p>
      {% endif %}
    </section>
  </main>
</body>
</html>
